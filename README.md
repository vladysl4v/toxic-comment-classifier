# Toxic Comment Classifier

This project builds machine learning models to classify toxic comments.

## Project structure

- `data/` - local datasets, not uploaded to GitHub
- `notebooks/` - experiments and EDA
- `src/` - reusable Python scripts
- `models/` - saved trained models
- `reports/` - figures and results

## Setup

```bash
pip install -r requirements.txt